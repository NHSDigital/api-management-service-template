# This is an OpenAPI Specification (https://swagger.io/specification/)
# for the Personal Demographics Service (PDS) API
# owned by NHS Digital (https://digital.nhs.uk/)
openapi: "3.0.0"
info:
  version: "{VERSION}"
  title: Example Test Service (FHIR) API
  description: |
    ## Overview

servers:
  - url: 'https://sandbox.api.service.nhs.uk/example-test'
    description: Sandbox environment.
  - url: 'https://int.api.service.nhs.uk/example-test'
    description: Integration test environment.
  - url: 'https://api.service.nhs.uk/example-test'
    description: 'Production environment.'

paths:
  /users:
    get:
      summary: Returns a list of users.
      description: Optional extended description in CommonMark or HTML.
      responses:
        '200':    # status code
          description: A JSON array of user names
          content:
            application/json:
              schema: 
                type: array
                items: 
                  type: string