type: object
properties:
  statusCode:
    type: integer
    description: Error code returned from server
    example: '400'
  error:
    type: string
    description: Error returned from server
    example: 'Bad Request'
  message:
    type: string
    description: Error message returned from the server
    example: 'Invalid params supplied'
